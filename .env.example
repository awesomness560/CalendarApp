# Frontend (Vite) - safe to expose
VITE_CLIENT_ID=

# Optional: only if frontend and API run on different origins (e.g. local Vite dev vs deployed API)
# VITE_API_URL=https://your-app.vercel.app

# Backend only (Vercel env vars – never commit real values)
# Set these in Vercel Dashboard → Project → Settings → Environment Variables
# GOOGLE_CLIENT_ID=     (same value as VITE_CLIENT_ID)
# GOOGLE_CLIENT_SECRET=